services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # This maps your local files to the container
      - ./tasks.json:/app/tasks.json
      - ./security.log:/app/security.log
    restart: always